1. When new image uploaded, drop out of edit mode
2. In edit mode, do not show upload for new image
3. Edit button not needed, clicking job should open edit mode for that job
4. Logs should persist, currently after reboot or just a previous job, log window only shows 1 line stating completed
5. Job "profiles". Allow a user to upload an image and filament and then select a preconfigured parameter set
6. Clicking a running job that isn't already selected does not load the right side properly
7. 
